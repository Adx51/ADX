# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository State

This is a new, empty repository (`adx51/ADX`). No language, framework, build tooling, or project structure has been established yet. When the project is initialized, update this file with:

- Build, lint, and test commands (including how to run a single test)
- Architecture overview and key design decisions
- Any non-obvious conventions or constraints

## Git Workflow

- Development branches follow the pattern `claude/<task-description>`
- Push to remote with: `git push -u origin <branch-name>`
- The default branch is `main`
